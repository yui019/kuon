1 + 23.12 else : 
"djskdjsjk "
val variable = true;

val a = 3; // const
var a = 3; // variable

var a = {
	var b = 2;
	b + 1
};

struct Person {
	name string,
	age uint,
}

fun makePerson(name string, age uint) struct { name string, age uint } {
	struct {
		name: "Kuon",
		age: 20,
	}
}

val person = getPerson();
// person.name = "Kuon", person.age = 20